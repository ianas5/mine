# Technical Debt Registry

Per IMPLEMENTATION_ROADMAP §2: every intentionally accepted shortcut is recorded
here at merge time with **why it exists · why it's acceptable now · when it must
be removed**. Checkpoints audit this registry; an overdue entry blocks the
checkpoint.

| ID | Introduced | Description | Why it exists | Why it's acceptable now | Must be removed by |
|---|---|---|---|---|---|
| TD-001 | Phase 0 | Physical-iPhone launch of the dev build not verified (only web-build rendering at iPhone dimensions). | Phases 0–2 were executed in a remote Linux environment with no macOS/iOS toolchain or device. | The shell is platform-thin; react-native-web exercised the same code paths; the data layer is verified against real SQLite in tests; risk is confined to native build config. | **CP-A** — owner runs `npx expo run:ios` on a physical iPhone and confirms: cold start, tab switching, theme follow, kill & relaunch, Settings screen walk (theme override + weekly target persist across relaunch), delete-app → reinstall → defaults return, stepper haptics/press feel, Dynamic Type 1.3×, **Exercise Library walk (browse/search/create custom/archive, 100+ scroll performance, both themes)**, **Active Workout gym walk (start → add 5 exercises → log 15+ sets at ≤2 taps each → warm-up toggle → keyboard entry → minimize & resume → finish/save and discard flows → both themes, stepper haptics)**, **History & prefill walk (log session A → start session B and confirm each re-added exercise prefills last-time's working sets and its in-card panel shows Last/Best/Best e1RM → open Recent list → open a workout's detail → edit a set's weight and confirm B's preview + Recent totals recompute → delete a set → delete the workout via the Dialog and confirm it leaves Recent and history everywhere → first-ever exercise shows the "set your baseline" state, no fabricated numbers, both themes)**, **Crash-safety & rest & session-bar walk (start a workout, log a few sets, then force-kill the app → relaunch and confirm the session is silently restored with the recovery banner offering Resume/Discard and the session bar docked above the tabs → complete a working set and confirm the rest timer auto-starts (90 s), background the app ~10 min mid-rest and confirm the countdown is still honest on return, Skip and +30 s work, a gentle haptic fires at zero → navigate every tab and confirm the session bar shows elapsed/current-exercise/rest countdown and one tap returns to the session → finish the workout and confirm no draft is left (relaunch shows no recovery) → discard flow likewise leaves no draft, both themes)**, **PRs & Exercise Report walk (log a set that beats a previous best → confirm the PR badge materializes on the row with a success haptic → finish and confirm the summary shows "N new PRs" and the toast reads "Workout saved · N PRs" → a tie logs no PR → open an exercise's report from the library actions sheet and confirm bests/totals/averages/last-performed match the workout history, the "trend coming" note is shown with no fake line → delete the record-holding workout and reopen the report to confirm the record has receded, both themes)**, **Programs & templates walk (build a PPL program with three sessions mapped to weekdays and exercises with targets → set it active → confirm the Workouts home shows the correct weekday session as "Suggested for today" and starting it is ≤ 2 taps with the exercises pre-loaded and targets visible on each card → confirm the template rest_seconds seeds the rest timer → repeat-last reloads the previous session's working sets → activate a second program and confirm the first deactivates (single active) → edit a template's exercises and confirm a past workout logged from it is unchanged → delete a template and confirm its past workout survives with provenance nulled, both themes)**, **Nutrition meal-logging walk (open Nutrition → Log Meal opens on Recent & Frequent with quick meals pinned → tap a food and confirm the portion is pre-filled with the last-used amount and the slot with the most-frequent/time-of-day default → Save in ≤ 3 taps for a repeat meal → confirm day totals update → delete a meal entry via the trash and confirm the 5 s Undo toast restores the identical row → navigate to a previous day and back → create a food, log it, then edit that food and confirm past entries keep their macros → delete a food and confirm its entries survive with the snapshot, both themes)**. |
| TD-002 | Phase 0 | The five placeholder screens duplicate their layout markup (~40 lines × 5). | A shared placeholder component would live in `core/ui`, which is deliberately empty until Phase 1; features must not import features. | Placeholders are disposable — each is deleted when its real screen lands (Phases 3–16); abstraction before a durable second consumer violates CODING_STANDARDS rule 9. | Naturally, as each phase replaces its placeholder; any still present at **CP-D** must be reviewed. |
| TD-004 | Phase 4 | ~~`workouts.template_id` is a plain nullable column without its FK to `templates`.~~ | — | — | ✅ **Resolved in Phase 8** — migration 0004 rebuilt `workouts` (SQLite table-rebuild pattern, DATABASE §5.4) with `template_id … REFERENCES templates(id) ON DELETE SET NULL`; SET NULL behaviour is tested. |
| TD-003 | Phase 1 | Swipe gestures are not yet wired: `Sheet` has no drag-to-dismiss grabber (DESIGN_SYSTEM §6), and destructive rows (meal entries, and later sets/photos) delete via a visible trash-tap rather than swipe-to-reveal (UI_UX §6). | A correct drag/swipe needs react-native-gesture-handler wiring and device-tuned physics that can't be validated in this environment. | Every action is fully reachable by visible taps, and deletes are immediate + 5 s Undo toast (forgiving, UI_UX §6) — the missing piece is gesture polish, not function; discoverability (§7.4) is satisfied by the visible controls. | The **Phase 21** feel pass (gesture-handler wiring for Sheet drag-dismiss + row swipe-to-reveal together). |
| TD-005 | Phase 5 | The Recent history list is a plain mapped list, not a virtualized FlashList (IMPLEMENTATION_ROADMAP Phase 5 names FlashList for history). | Recent is capped at 20 rows inside the Workouts-home `ScrollView`; a single-user dataset is in the low hundreds of sessions, so virtualization has no user-visible benefit and would add a dependency + gesture surface. | No perceptible cost at this data scale; building virtualization before a durable large-list consumer violates the Rule of Two (CODING_STANDARDS rule 9). | When a full-screen **all-history archive** screen is built (unscheduled; before it ships that list must use FlashList). |
| TD-006 | Phase 6 | The dashboard-side Focus-Mode subtractions (UI_UX §5.1: hide insight cards, workout card → live session card, slim quick actions to Return-to-Workout) are not implemented; only the app-wide **session bar** — the core §5.1 mechanism — was built. | The Dashboard is still a placeholder until Phase 16; there are no insight/quick-action cards to subtract yet, so those behaviours cannot be written against real UI. | The session bar delivers §5.1's essential promise (session docked app-wide, one-tap return) and "no non-session toasts while Active Workout is foregrounded" holds by construction (the active screen is a full-screen modal that only speaks session/rest feedback). | **Phase 16** — the Dashboard must be built Focus-Mode-aware (insight cards hidden mid-session, live session card, Return-to-Workout quick action). |
| TD-007 | Phase 7 | The Exercise Report is **all-time only** and omits three parts of the §5.5 contract: the e1RM trend + progression rate (already roadmap-accepted debt), a **selected-range** view of sessions/volume/averages, and **recent notes** mentioning the exercise. The screen states on-screen that no trend is shown (P8, no fake trend). | Trend, progression rate and range-scoping are the same time-series/regression family the roadmap defers to Phase 15; recent-notes surfacing needs a notes-search path not yet built. Bests/PRs are inherently all-time, which is the trustworthy default. | Every number shown is exact and reconciles with history detail; nothing is fabricated (P8). The missing pieces are additive analytics, not correctness gaps. | **Phase 15** — add the e1RM trend chart, progression rate, selected-range metrics, and recent-notes to complete the §5.5 contract. |
