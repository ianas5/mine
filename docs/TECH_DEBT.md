# Technical Debt Registry

Per IMPLEMENTATION_ROADMAP §2: every intentionally accepted shortcut is recorded
here at merge time with **why it exists · why it's acceptable now · when it must
be removed**. Checkpoints audit this registry; an overdue entry blocks the
checkpoint.

| ID | Introduced | Description | Why it exists | Why it's acceptable now | Must be removed by |
|---|---|---|---|---|---|
| TD-001 | Phase 0 | Physical-iPhone launch of the dev build not verified (only web-build rendering at iPhone dimensions). | Phases 0–2 were executed in a remote Linux environment with no macOS/iOS toolchain or device. | The shell is platform-thin; react-native-web exercised the same code paths; the data layer is verified against real SQLite in tests; risk is confined to native build config. | **CP-A** — owner runs `npx expo run:ios` on a physical iPhone and confirms: cold start, tab switching, theme follow, kill & relaunch, Settings screen walk (theme override + weekly target persist across relaunch), delete-app → reinstall → defaults return, stepper haptics/press feel, Dynamic Type 1.3×, **Exercise Library walk (browse/search/create custom/archive, 100+ scroll performance, both themes)**, **Active Workout gym walk (start → add 5 exercises → log 15+ sets at ≤2 taps each → warm-up toggle → keyboard entry → minimize & resume → finish/save and discard flows → both themes, stepper haptics)**, **History & prefill walk (log session A → start session B and confirm each re-added exercise prefills last-time's working sets and its in-card panel shows Last/Best/Best e1RM → open Recent list → open a workout's detail → edit a set's weight and confirm B's preview + Recent totals recompute → delete a set → delete the workout via the Dialog and confirm it leaves Recent and history everywhere → first-ever exercise shows the "set your baseline" state, no fabricated numbers, both themes)**, **Crash-safety & rest & session-bar walk (start a workout, log a few sets, then force-kill the app → relaunch and confirm the session is silently restored with the recovery banner offering Resume/Discard and the session bar docked above the tabs → complete a working set and confirm the rest timer auto-starts (90 s), background the app ~10 min mid-rest and confirm the countdown is still honest on return, Skip and +30 s work, a gentle haptic fires at zero → navigate every tab and confirm the session bar shows elapsed/current-exercise/rest countdown and one tap returns to the session → finish the workout and confirm no draft is left (relaunch shows no recovery) → discard flow likewise leaves no draft, both themes)**, **PRs & Exercise Report walk (log a set that beats a previous best → confirm the PR badge materializes on the row with a success haptic → finish and confirm the summary shows "N new PRs" and the toast reads "Workout saved · N PRs" → a tie logs no PR → open an exercise's report from the library actions sheet and confirm bests/totals/averages/last-performed match the workout history, the "trend coming" note is shown with no fake line → delete the record-holding workout and reopen the report to confirm the record has receded, both themes)**, **Programs & templates walk (build a PPL program with three sessions mapped to weekdays and exercises with targets → set it active → confirm the Workouts home shows the correct weekday session as "Suggested for today" and starting it is ≤ 2 taps with the exercises pre-loaded and targets visible on each card → confirm the template rest_seconds seeds the rest timer → repeat-last reloads the previous session's working sets → activate a second program and confirm the first deactivates (single active) → edit a template's exercises and confirm a past workout logged from it is unchanged → delete a template and confirm its past workout survives with provenance nulled, both themes)**, **Nutrition meal-logging walk (open Nutrition → Log Meal opens on Recent & Frequent with quick meals pinned → tap a food and confirm the portion is pre-filled with the last-used amount and the slot with the most-frequent/time-of-day default → Save in ≤ 3 taps for a repeat meal → confirm day totals update → delete a meal entry via the trash and confirm the 5 s Undo toast restores the identical row → navigate to a previous day and back → create a food, log it, then edit that food and confirm past entries keep their macros → delete a food and confirm its entries survive with the snapshot, both themes)**, **Targets, adherence & water walk (set targets from today → confirm the day view shows consumed/target/remaining per macro with adherence coloring and the protein floor vs calorie/carb/fat bands → log meals and confirm remaining updates, including going negative/over → set new targets effective tomorrow and confirm today's numbers are unchanged → open a past day before the first target and confirm it reads "no target set" (insufficient-data, not a fabricated default) → add/remove water by the configured cup and confirm a logged 0 reads differently from an unlogged day, both themes)**, **Measurements walk (Add Weight opens with the stepper pre-set to the last weight → adjust → Save in ≤ 3 taps → confirm it appears in the weight log with the correct delta from the previous weigh-in → Add Measurements with the frequently-logged fields expanded and the rest behind "More sites", fill a partial subset → Save → confirm the current-state card updates and BMI shows entered-else-derived → log a second same-date entry that omits a field and confirm the omitted field keeps its value (omit ≠ clear) → explicitly clear one field and confirm only that field is removed → confirm bilateral sites show per side, both themes)**, **Measurement comparison walk (with ≥ 2 measurement days, open Compare → confirm the date pickers list only snapshot dates and default to the two most recent → confirm each field present on both dates shows Δ, %Δ, and a direction glyph coloured by §5.3 (waist down = good/green, arm down = bad/red, weight/BMI neutral) → confirm a sub-deadband change reads "stable" → confirm a field present on only one of the two dates shows "—" with no fabricated baseline → change a picker date and confirm the table recomputes, both themes)**, **Progress-photos walk (open Measurements → Progress photos → Add Photo and confirm the angle chip defaults to the oldest-missing angle (front on an empty gallery) → capture with the camera (grant permission) and choose from the library, confirming the file is copied into the app's photos dir and appears in the timeline grouped by date, newest first, with an angle badge → tap a thumbnail to view it full-size → delete it via the Dialog and confirm both the row and its file are gone → force-kill the app during a save (between file-write and row-insert) then relaunch and confirm the orphan sweep removed the stray file with no half-photo shown → with ≥ 2 photos of one angle open Compare, confirm angle chips list only captured angles, Before defaults to the earliest and After to the latest, side-by-side renders both, and the Before/After segmented toggle swaps a single full-width image with a date label → confirm a photo whose file is missing renders the "File missing" placeholder instead of a broken image, both themes)**, **Backup / export / import walk (Settings → Backup → Export backup produces `fitness-backup-YYYY-MM-DD.zip` and opens the OS share sheet; save it to Files and confirm it contains `data.json` + a `photos/` folder with the real images → Import backup opens the document picker, and picking that zip shows the "Replace all data?" confirmation → confirm and verify every screen — workouts, history/PRs, programs/templates, nutrition/targets/water, measurements, progress photos — is fully restored including image bytes, and that a `pre-import-safety-…zip` was written first → delete the app, reinstall, and import the saved backup to confirm a clean-device restore brings back everything incl. photos → import a deliberately corrupted/truncated zip and a non-backup file and confirm each shows a clear error with all existing data left intact → simulate a failed safety export (e.g. no free space / no share target) and confirm the "Safety backup failed — continue?" confirmation appears and that declining leaves data untouched, both themes)**, **Analytics Body-section walk (open Analytics → confirm the range control switches 7D/30D/3M/6M/1Y/All and the weight chart + stats recompute → with ≥ 3 weigh-ins over ≥ 14 days confirm the Victory Native XL weight line renders inside its ChartFrame with the interpretation sentence, the dashed goal line at the target weight, and a "trend weight" StatTile whose context reads the direction/rate → set a goal weight in Settings and confirm distance-to-target shows kg-to-go with a "~N wk at pace" ETA that disappears when the trend is stable or heading away → clear the goal and confirm distance reads "Set a goal weight" and the weight classification goes neutral → open a sparse range and confirm the chart is replaced by the honest "not enough weigh-ins" message with no fabricated line → confirm the waist StatTile trends with lower-is-better colouring → open an Exercise Report with ≥ 3 sessions over ≥ 14 days and confirm the Strength-trend card shows the e1RM sparkline + progression rate (kg/week), and that an exercise below the minimums shows the "needs more data" sentence instead, both themes)**, **Dashboard daily-briefing walk (open the Dashboard and confirm the 10-second briefing reads top-to-bottom: header greeting with the trend weight, today's workout card in the right state (planned shows the suggested session, done after a countable workout today, rest when nothing scheduled), calorie & protein rings + carb/fat bars from today's target, the weekly streak line with "N of M this week", and the fixed-order quick actions — Start · Log Meal · Weight · Measure · Photo → confirm the ordering swaps by daypart: morning (before ~11:00) puts the workout card above macros and the greeting shows the weigh-in prompt when unweighed; afternoon/evening puts remaining calories/protein first → tap each quick action and confirm it reaches its sheet in one tap (Log Meal, Add Weight, Measure open their sheets; Photo opens the photos screen; Start opens Workouts) → start a workout, return to the Dashboard, and confirm Focus Mode: the live session card is first, the insight slot is hidden, and quick actions slim to Return · Log Meal · Weight → finish and confirm the normal layout returns → confirm the streak matches reality across a week boundary (Monday resets to "0 of M" as progress, never a crash), both themes)**, **Analytics Training + Muscle Report walk (open Analytics → Training and confirm it reads as coaching answers, not a totals dump: a Consistency tile (streak / this-week progress / frequency), a "Getting stronger?" summary listing the main lifts with an up/down verdict or "needs more sessions", Push:Pull and Upper:Lower balance tiles that flag amber when outside their bands (0.8–1.25 and 1.0–2.0), a Most/Least-trained-in-30d pair that flags a neglected group, and a single weekly-volume ChartFrame with its interpretation line → switch ranges and confirm every number recomputes and the previous-period comparisons make sense → open the Muscle report and confirm each trained group reads like a coach's review (volume/sets/frequency, strongest lift by e1RM, fastest-improving lift, last-trained recency) and that never-trained groups appear honestly under "Not trained in this range" with no fabricated figures → cross-check a couple of numbers (a group's 30-day volume, a lift's e1RM) against the workout history, and confirm unilateral single-logged exercises show doubled volume, both themes)**. |
| TD-002 | Phase 0 | ~~Placeholder tab screens duplicate their layout markup.~~ | — | — | ✅ **Resolved in Phase 16** — the Dashboard was the last placeholder; all five tabs (Dashboard, Workouts, Nutrition, Measurements, Analytics) are now real screens. No placeholder markup remains. |
| TD-004 | Phase 4 | ~~`workouts.template_id` is a plain nullable column without its FK to `templates`.~~ | — | — | ✅ **Resolved in Phase 8** — migration 0004 rebuilt `workouts` (SQLite table-rebuild pattern, DATABASE §5.4) with `template_id … REFERENCES templates(id) ON DELETE SET NULL`; SET NULL behaviour is tested. |
| TD-003 | Phase 1 | Swipe gestures are not yet wired: `Sheet` has no drag-to-dismiss grabber (DESIGN_SYSTEM §6), and destructive rows (meal entries, and later sets/photos) delete via a visible trash-tap rather than swipe-to-reveal (UI_UX §6). | A correct drag/swipe needs react-native-gesture-handler wiring and device-tuned physics that can't be validated in this environment. | Every action is fully reachable by visible taps, and deletes are immediate + 5 s Undo toast (forgiving, UI_UX §6) — the missing piece is gesture polish, not function; discoverability (§7.4) is satisfied by the visible controls. | The **Phase 21** feel pass (gesture-handler wiring for Sheet drag-dismiss + row swipe-to-reveal together). |
| TD-005 | Phase 5 | The Recent history list is a plain mapped list, not a virtualized FlashList (IMPLEMENTATION_ROADMAP Phase 5 names FlashList for history). | Recent is capped at 20 rows inside the Workouts-home `ScrollView`; a single-user dataset is in the low hundreds of sessions, so virtualization has no user-visible benefit and would add a dependency + gesture surface. | No perceptible cost at this data scale; building virtualization before a durable large-list consumer violates the Rule of Two (CODING_STANDARDS rule 9). | When a full-screen **all-history archive** screen is built (unscheduled; before it ships that list must use FlashList). |
| TD-006 | Phase 6 | ~~Dashboard-side Focus-Mode subtractions not implemented.~~ | — | — | ✅ **Resolved in Phase 16** — the Dashboard is Focus-Mode-aware (UI_UX §5.1): during an active session the workout card becomes the live session card (shown first), the insight slot is hidden, and quick actions slim to Return-to-Workout · Log Meal · Add Weight. The active-session summary is passed from the app route so no feature-boundary is crossed. |
| TD-007 | Phase 7 | ~~The Exercise Report omits the e1RM trend + progression rate~~ (✅ **added in Phase 15** — the Strength-trend card shows the e1RM sparkline + progression rate, or the honest "needs more data" state below §6.4 minimums). Still open: a **selected-range** view of the report's sessions/volume/averages (currently all-time only), and **recent notes** mentioning the exercise. | Range-scoping the report body and surfacing note mentions are additive UI over paths not yet built (a notes search); the all-time bests remain the trustworthy default. | Every number shown is exact and reconciles with history; the trend is now live and honest. The remaining pieces are additive, not correctness gaps. Phase 17 delivered the range-scoped **Training analytics + Muscle Report** (§5.1/§5.6), but the per-exercise Report *screen body* is still all-time. | An **unscheduled Exercise-Report pass** (or the Phase 20 smart-defaults/report polish) — add the selected-range report body and recent-notes to finish the §5.5 contract. |
| TD-008 | Phase 11 | Multi-field entry sheets (Add Measurements, and the food/target editors) set `returnKeyType="next"` but do not yet advance focus to the next field on return — full keyboard-next chaining (UI_UX §4.5/§5.3) is not wired. | True focus-advance across a dynamic field list needs `Input` to forward a ref and a per-sheet ref chain; it is device-feel work that can't be validated on web. | Every field is reachable and fillable by tap; partial entry + merge-upsert works fully. The missing piece is keyboard-flow polish, not function. | The **Phase 21** keyboard-first feel pass (forward `Input` refs + wire next-field chaining across the multi-field sheets). |
| TD-009 | Phase 11 | ~~Bodyweight-load volume resolved bodyweight from `settings.defaultBodyweightKg`, not the latest weigh-in.~~ | — | — | ✅ **Resolved in Phase 16** — `useDefaultBodyweight` now prefers the latest `body_snapshots` weigh-in (FITNESS_DOMAIN §5.2 canonical), falling back to `settings.defaultBodyweightKg` then null, via the new `bodyRepository.getLatestWeightKg()`; reactive to both body and settings writes. |
| TD-010 | Phase 15 | The `TrendChart` (Victory Native XL) implements the essential DESIGN_SYSTEM §6.1 rules (accent 2.5 pt line, dashed target reference, engine-bucketed data) but not yet the full set: no soft area fill @ 8%, no dashed regression overlay, no single-tap tooltip. | The chart renders on a Skia canvas that cannot be launched or visually verified in this Linux/remote environment; building elaborate Skia styling blind risks shipping unverifiable, wrong-looking output. The interpretation contract (ChartFrame line, StatTile context) is already met, so the missing pieces are visual polish, not analytical honesty. | Every conclusion is carried in text (ChartFrame interpretation + StatTiles), so the chart is never the sole signal (P8); the line + target already answer "which way and how far from goal." | The **Phase 21** feel pass, or the first checkpoint after the charts are device-verified under TD-001 — add area fill, the dashed regression overlay, and the tap tooltip per §6.1. |
