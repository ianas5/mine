/** Semantic primitive aliases (CODING_STANDARDS §1.7). */

/** A device-local calendar date, `YYYY-MM-DD` (FITNESS_DOMAIN §2.3). */
export type IsoDate = string;

/** A Unix epoch timestamp in milliseconds (UTC), audit-only. */
export type EpochMs = number;
